# VAXIS - Universal Reality Forge
# 依存パッケージ一覧 (requirements.txt)
#
# このファイルは setup_helper.py が参照する依存関係の定義です。
# 手動インストール時は以下のコマンドを使用してください:
#   blender --background --python-expr \
#     "import subprocess, sys; subprocess.run([sys.executable, '-m', 'pip', 'install', '-r', 'requirements.txt'])"
#
# ==========================================================
# フェーズ1 (現在): UI基盤
# ==========================================================
# 現フェーズでは依存パッケージは不要。
# 将来のフェーズ2 (AI統合) に備えて事前インストールする。
#
# ==========================================================
# フェーズ2 (予定): AI API統合
# ==========================================================

# HTTP通信: SF3D API / Panorama360-XL との通信
requests>=2.31.0

# 画像処理: テクスチャ変換・HDRI編集
Pillow>=10.0.0

# 数値計算: メッシュデータ・頂点配列の高速処理
numpy>=1.24.0

# 進捗表示: 生成処理中のプログレスバー
tqdm>=4.65.0
